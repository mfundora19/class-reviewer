window.ReviewApp.content.register({
type: "notes",
cert: "linux-plus",
chapter: "Ch 04 · Processing and Editing Text",
items: [
{
title: "Text Processing, Filtering, Formatting, and Redirection",
body: "# Linux+ Ch 04 — Text Processing\n\n## Core Utilities\n\n| Command | Primary use | Key syntax/options |\n|---|---|---|\n| `cut` | Extract fields, characters, or bytes without modifying the source | `-c` characters, `-b` bytes, `-d` delimiter, `-f` fields, `-s` only delimited records, `-z` NUL-terminated records |\n| `grep` | Search records for a pattern | `-c` count, `-i` ignore case, `-r`/`-R` recursive, `-v` invert, `-E` ERE |\n| `sort` | Sort input for display; source file is unchanged | `-n` numeric, `-r` reverse, `-u` unique, `-k` key/field, `-t` separator, `-o` output file, `-f` ignore case, `-M` month, `-V` version |\n| `uniq` | Filter adjacent duplicate lines | `-c` count, `-d` duplicates only, `-u` non-repeated only, `-i` ignore case, `-f N` skip fields, `-s N` skip characters |\n| `cat` | Display/concatenate files | `-n` number lines, `-b` number nonblank, `-E` show `$` at line ends, `-T` show TAB as `^I`, `-v` show nonprinting, `-A` = `-vET` |\n| `fmt` | Reflow/wrap text paragraphs | `-w 72` sets width; default is 75 in the notes |\n| `printf` | Formatted output | `%s`, `%d`, `%f`, `%c`, `%%`; `\\n`, `\\t`, `\\r`, `\\f`, `\\\"`, `\\\\` |\n| `wc` | Text statistics | default: lines, words, bytes; `-l`, `-w`, `-c`, `-m`, `-L` |\n| `diff` | Compare two files line by line | `-u` unified, `-y` side-by-side, `-i` ignore case, `-w` ignore whitespace, `-e` ed script |\n| `tee` | Save pipeline output while also displaying it | `-a` append, `-i` ignore interrupts |\n\n## `cut`\n\nA text-file **record** is a line ending in LF. Fields are simply data separated by a delimiter; `/etc/passwd` uses `:`. `cut` only copies selected data for display and does not modify the input file.\n\n`bash\ncut -d \":\" -f 1,7 /etc/passwd\ncut -c 1-5 /etc/passwd\n`\n\n`-z` is used when records end in ASCII NUL rather than LF.\n\n## `grep` + Regular Expressions\n\n### Basic regex (BRE)\n\n| Pattern | Meaning |\n|---|---|\n| `.` | Any single character |\n| `.*` | Zero or more characters in the pattern usage shown |\n| `[abc]` | Any one listed character |\n| `[^abc]` | Any character not listed |\n| `^` | Start of line |\n| `$` | End of line |\n| `*` | Zero or more occurrences of the preceding character |\n| `{n}` | Exactly `n` occurrences |\n| `{n,m}` | `n` through `m` occurrences |\n\nExamples:\n\n`bash\ngrep daemon.*nologin /etc/passwd\ngrep ^root /etc/passwd\ngrep -v nologin$ /etc/passwd\n`\n\n`grep -v nologin$` is useful for showing records whose shell is **not** `/sbin/nologin`.\n\n### Extended regex (ERE)\n\nUse `grep -E`. The notes describe:\n\n| Pattern | Meaning |\n|---|---|\n| `+` | One or more of the preceding character |\n| `?` | Zero or one of the preceding character |\n| `{n}` | Exactly `n` occurrences |\n| `{n,m}` | `n` to `m` occurrences |\n| `|` | Alternation (OR) |\n| `()` | Grouping/subexpression |\n\n`egrep` is equivalent to `grep -E`. Without `-E`, ERE syntax such as `+` or `|` is treated as literal text instead of the intended ERE operators.\n\n## Sorting + Deduplication\n\n`uniq` detects **adjacent** duplicates, so it is normally preceded by `sort`:\n\n`bash\nsort access.log | uniq -c | sort -rn\nsort names.txt | uniq -d\nsort names.txt | uniq -u\n`\n\nExample: sort `/etc/passwd` by UID (field 3) numerically:\n\n`bash\nsort -t: -k3 -n /etc/passwd\n`\n\n## `cat`, `fmt`, `printf`, and `wc`\n\n`cat -A` is useful for debugging invisible characters. For example, TAB may appear as `^I`, and nonprinting characters can appear in caret notation.\n\n`printf` does **not** automatically add a newline, so include `\\n` explicitly:\n\n`bash\nprintf \"%s\\n\" \"Hello World\"\nprintf \"Pi is approximately %.2f\\n\" 3.14159\n`\n\n`wc` defaults to **lines, words, bytes**. `wc -L` reports the byte count of the longest line and can help spot a configuration line that was accidentally merged with another.\n\n## Standard Streams + Redirection\n\n| Stream | Descriptor | Default |\n|---|---:|---|\n| STDIN | `0` | Keyboard/input source |\n| STDOUT | `1` | Terminal (`/dev/tty`) |\n| STDERR | `2` | Terminal (`/dev/tty`) |\n\n| Operator | Effect |\n|---|---|\n| `>` | Redirect STDOUT; overwrite/create |\n| `>>` | Redirect STDOUT; append/create |\n| `2>` | Redirect STDERR; overwrite/create |\n| `2>>` | Redirect STDERR; append/create |\n| `&>` | Redirect STDOUT + STDERR; overwrite/create |\n| `&>>` | Redirect STDOUT + STDERR; append/create |\n| `2>&1` | Send STDERR to the current STDOUT destination |\n| `<` | Redirect STDIN from a file |\n| `<<` | Here document; multiple lines become STDIN |\n| `<<<` | Here string; one string becomes STDIN |\n| `<>` | Redirect STDIN from a file and STDOUT to the specified file |\n\n> **Exam warning:** `>` can destroy existing contents. Use `>>` when appending.\n\n`/dev/null` is the **bit bucket**: data redirected there is discarded.\n\n## Pipes and `tee`\n\nA pipe `|` sends one command's STDOUT into the next command's STDIN.\n\n`bash\ngrep \"/bin/bash\" /etc/passwd | wc -l\ngrep \"root\" /etc/passwd | cut -d: -f1 | sort\n`\n\n`tee` saves piped output while still displaying it:\n\n`bash\ngrep \"error\" /var/log/syslog | tee errors.txt\n`\n\nIt can also continue through another pipe:\n\n`bash\ncommand | tee output.txt | grep \"error\"\n`\n\n## Here Documents\n\n`bash\ncommand <<EOF\nline 1\nline 2\nline 3\nEOF\n`\n\nQuoting the delimiter, such as `'EOF'` or `\"EOF\"`, suppresses variable expansion inside the here document. Without quotes, variables such as `$HOME` are expanded.\n\n## `xargs`, Command Substitution, and Brace Expansion\n\n`xargs` reads items from STDIN and turns them into command arguments.\n\n| Option | Purpose |\n|---|---|\n| `-p` | Prompt before execution |\n| `-n NUM` | At most NUM arguments per command |\n| `-0` | NUL-delimited input |\n| `-I STR` | Replace `STR` with input items |\n| `-t` | Print command to STDERR before execution |\n\nFor filenames containing spaces, pair `find -print0` with `xargs -0`.\n\nCommand substitution:\n\n`bash\n$(command)\n`\n\nis preferred over older backtick syntax:\n\n``bash\n`command`\n``\n\nBrace expansion is a Bash feature, not a command:\n\n`bash\nfile{1,2,3}\n{1..5}\n{a..e}\ndir/{a,b}/{c,d}\n`\n\n## `diff`\n\n`bash\ndiff file1 file2\ndiff -u old.conf new.conf > changes.patch\npatch original.txt < changes.patch\n`\n\n`-u` is unified output, `-y` is side-by-side output, `-w` ignores all whitespace, `-i` ignores case, and `-e` creates an ed script.\n",
tags: ["grep", "cut", "sort", "uniq", "redirection", "pipes", "regex", "wc"]
},
{
title: "Text Editing: nano, Vim, sed, and awk",
body: "# Linux+ Ch 04 — Text Editing Reference\n\n## `nano`\n\n`nano` is the simple interactive editor emphasized for straightforward text modifications.\n\n| Shortcut | Action |\n|---|---|\n| `Ctrl+O` | Write/save |\n| `Ctrl+X` | Exit |\n| `Ctrl+K` | Cut current line |\n| `Ctrl+U` | Paste cut text |\n| `Ctrl+W` | Search |\n| `Ctrl+G` | Help |\n\nThe interface has a **Title Bar**, **Main Body**, **Status Bar**, and **Shortcut List**. A `^` in the shortcut list means the Ctrl key.\n\n## `vi` / `vim`\n\n`vim` means **vi improved**. On many systems `vi` starts `vim`, but some distributions may provide `vim.tiny` instead of the full Vim implementation.\n\n`bash\nwhich vim\nwhich vi\ntype vi\nreadlink -f <path>\n`\n\n### Vim Modes\n\n1. **Command/Normal mode** — initial mode; movement and command keystrokes.\n2. **Insert mode** — enter with `I`; leave with `Esc`.\n3. **Ex mode** — colon commands such as `:q`; enter from command mode.\n\n### Command Mode Essentials\n\n| Key | Action |\n|---|---|\n| `h` / `j` / `k` / `l` | Left / down / up / right |\n| `w` | Forward one word |\n| `b` | Backward one word |\n| `0` | Beginning of line |\n| `$` | End of line |\n| `gg` | First line of file |\n| `G` | Last line of file |\n| `nG` | Line `n` |\n| `Ctrl+f` | Full screen down |\n| `Ctrl+b` | Full screen up |\n| `Ctrl+d` | Half screen down |\n| `Ctrl+u` | Half screen up |\n| `x` | Delete character under cursor |\n| `dd` | Delete current line |\n| `yy` | Yank/copy current line |\n| `p` | Paste after cursor |\n| `u` | Undo |\n| `Ctrl+r` | Redo |\n| `/pattern` | Search forward |\n| `?pattern` | Search backward |\n| `n` / `N` | Repeat search same/opposite direction |\n\n### Vim Substitution\n\n`vim\n:s/old/new/g\n:%s/old/new/g\n`\n\n`:s` operates on the current line; `:%s` applies the substitution to the entire file. `g` replaces all occurrences on each affected line.\n\n### Vim Ex Commands\n\n| Command | Action |\n|---|---|\n| `:x` | Write and quit |\n| `:wq` | Write and quit |\n| `:wq!` | Write and quit, overriding protection |\n| `:w` | Write and stay |\n| `:w!` | Write, overriding protection |\n| `:q` | Quit without writing |\n| `:q!` | Quit without writing, overriding protection |\n| `:! command` | Run shell command and display results |\n| `:r! command` | Run shell command and insert results into buffer |\n| `:r file` | Read file into buffer |\n\n`ZZ` in command mode writes the buffer and exits. `vimtutor` launches the Vim tutorial when installed.\n\n## `sed` — Stream Editing\n\n`sed` reads input line by line, applies editing commands, and outputs the transformed text.\n\n| Option | Purpose |\n|---|---|\n| `-e script` | Add script expression |\n| `-f script-file` | Read commands from file |\n| `-i` | Edit in place |\n| `-n` | Suppress automatic printing |\n\nMost common form:\n\n`bash\nsed 's/pattern/replacement/flags' file.txt\n`\n\n### Substitution Flags\n\n| Flag | Meaning |\n|---|---|\n| `g` | Replace all occurrences on each line |\n| `n` | Replace only the nth occurrence on each line |\n| `p` | Print line if substitution occurred |\n| `w FILE` | Write changed line to FILE |\n| `I` | Ignore case |\n\nExamples:\n\n`bash\necho \"I like cake\" | sed 's/cake/donut/'\nsed 's/http/https/g' website.conf\nsed '/error/d' logfile.txt\nsed -n '5,10p' largefile.txt\n`\n\n> **Safety:** `-i` modifies the original file. Test the expression without `-i` first; `-i.bak` can create a backup before modification.\n\n## `awk` / `gawk`\n\n`awk` is a programming language for more complex field-based processing, calculations, and formatted reports. On many distributions, `awk` invokes GNU `gawk`.\n\n`bash\nawk [OPTIONS] 'pattern { action }' [FILE...]\n`\n\n### Core `awk` Variables\n\n| Variable | Meaning |\n|---|---|\n| `$0` | Entire current line |\n| `$1`, `$2`, ... | Individual fields |\n| `NR` | Current line number |\n| `NF` | Number of fields |\n\nFields are separated by **whitespace by default**.\n\n| Option | Purpose |\n|---|---|\n| `-F SEP` | Set field separator |\n| `-f FILE` | Read awk program from a file |\n| `-v VAR=VAL` | Set variable before execution |\n\nExamples:\n\n`bash\nawk -F: '{print $1, $6}' /etc/passwd\nawk -F: '$3 > 1000 {print $1}' /etc/passwd\nawk -F: 'END {print NR}' /etc/passwd\nawk '{sum += $1} END {print \"Total:\", sum}' numbers.txt\n`\n\n## Choosing the Right Tool\n\n| Need | Best fit |\n|---|---|\n| Find lines matching a pattern | `grep` |\n| Make simple stream substitutions | `sed` |\n| Process fields, calculate, or create structured output | `awk` |\n| Extract specific fields/characters | `cut` |\n| Sort records | `sort` |\n| Count adjacent duplicates | `uniq` |\n| Count lines/words/bytes | `wc` |\n| Compare two files | `diff` |\n| Save pipeline output while displaying it | `tee` |\n",
tags: ["nano", "vim", "vi", "sed", "awk", "text-editors", "stream-editors"]
}
]
});
